package resolution

var width int = 400
var height int = 600

func Resolution() {
	// app := app.New()
	// window := app.NewWindow("resolution")
	// window.Resize(fyne.NewSize(width, height))
	// wInput := widget.NewEntry()
	// wInput.Set

	// window.ShowAndRun()
}
